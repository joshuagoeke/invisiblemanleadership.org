{% extends '_base.twig' %}

{% block main %}
    {% if entry.exampleInput %}
        {{ entry.exampleInput }}
    {% endif %}

    {% set blocks = entry.blocks.all() %}
    {% if blocks|length %}
        {% for block in blocks %}
            {% switch block.type %}
            {% case "navbar" %}
                {% include '_modules/navbar.twig' with {
                    logo: block.logo,
                    customLinks: block.customLinks,
                    giveButtom: block.giveButton,
                } %}
            {% case "stats" %}
                {% include '_modules/stats.twig' with {
                    title: block.statsTitle,
                    stats: block.statsList,
                } %}
            {% case "foobar" %}
                {{ block.foobarTitle }}
            {% endswitch %}
        {% endfor %}
    {% endif %}

{% endblock %}
